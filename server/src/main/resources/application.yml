spring:
  datasource:
    url: "jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres"
    username: "postgres.fqxyocolbkxjsohpxvpg"
    password: "wavsox-guhrah-6kidJi"
    # url: "jdbc:postgresql://localhost:5432/smartstocks"
    # username: "smartuser"
    # password: "smartpass"
    driverClassName: "org.postgresql.Driver"

  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: "false"
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: "false"
        dialect: "org.hibernate.dialect.PostgreSQLDialect"

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/actuator"
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html

# OpenTelemetry Configuration
otel:
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}
      protocol: http/protobuf
  resource:
    attributes:
      service.name: ${OTEL_SERVICE_NAME:smart-stocks-backend}
      service.version: ${OTEL_SERVICE_VERSION:1.0.0}
