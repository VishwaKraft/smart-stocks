spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/smartstocks}
    username: ${SPRING_DATASOURCE_USERNAME:smartuser}
    password: ${SPRING_DATASOURCE_PASSWORD:smartpass}
    driverClassName: "org.postgresql.Driver"
    hikari:
      data-source-properties:
        prepareThreshold: 0

  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: ${SPRING_JPA_SHOW_SQL:false}
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: "false"
        dialect: "org.hibernate.dialect.PostgreSQLDialect"

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/actuator"
  endpoint:
    health:
      show-details: always

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  short-link:
    base-url: ${SHORT_LINK_BASE_URL:http://localhost:8080/s/}
  tracking:
    base-url: ${TRACKING_BASE_URL:http://localhost:8080/tracking}

# OpenTelemetry Configuration
# otel:
#   exporter:
#     otlp:
#       endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}
#       protocol: http/protobuf
#   resource:
#     attributes:
#       service.name: ${OTEL_SERVICE_NAME:smart-stocks-backend}
#       service.version: ${OTEL_SERVICE_VERSION:1.0.0}

aws:
  s3:
    accessKey: ${AWS_S3_ACCESS_KEY:8eaef1bdbe04162a1e98f1bd39fb7bf8}
    secretKey: ${AWS_S3_SECRET_KEY:94a8e8cfd7718ad9e6e72f7d4893959309df7621dd991fc98bb853b5912c48f9}
    region: ${AWS_S3_REGION:us-east-1}
    bucket: ${AWS_S3_BUCKET:campaign-segments}
    endpoint: ${AWS_S3_ENDPOINT:https://nmzidzfsrwmvhxhbgfzs.storage.supabase.co/storage/v1/s3}

gemini:
  api:
    key: ${GEMINI_API_KEY:}
